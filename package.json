{
  "name": "stocks-mongo-api",
  "version": "1.0.0",
  "description": "access stocks, futures, commodity, crypto, etc info from a mongodb",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "nick r",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^3.6.9"
  }
}
